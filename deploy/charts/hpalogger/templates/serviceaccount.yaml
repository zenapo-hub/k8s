{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "hpalogger.serviceAccountName" . }}
  labels:
    {{- include "hpalogger.labels" . | nindent 4 }}
{{- end }}
